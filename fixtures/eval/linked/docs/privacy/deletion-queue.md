# Deletion Queue

The deletion queue tracks pending erasure jobs, retry state, audit evidence, and completion timestamps.
It turns a policy commitment into an operational workflow with a clear owner.
