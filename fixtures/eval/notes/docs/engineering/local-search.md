# Local Semantic Search

Local semantic search keeps private notes and project context on the user's
machine. The index can combine embeddings, keyword search, and literal matching
without sending the corpus to a cloud service.

