# Shell Wrappers

Shell wrappers can route short flagless commands into dora while preserving the
normal behavior of grep, rg, ag, and find when users pass flags or ask for exact
filesystem traversal.

