# Privacy Default

Dora treats local-only operation as the default because notes and code often
contain private context. Cloud embeddings are optional; the normal path keeps the
corpus, vectors, and usage logs on the user's machine.

