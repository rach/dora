# Embedders

The default embedder runs locally. Users can configure OpenAI embeddings when
they want cloud quality or a specific model, but that choice should be explicit
and easy to estimate before indexing.

